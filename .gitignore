node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.*
.DS_Store
Thumbs.db
*.log

# Originaldateien vom Handy - kommen nicht ins Repository,
# sondern gehen durch `npm run aufbereiten`.
roh/

# Metadaten im Klartext (mit genauen GPS-Koordinaten) bleiben lokal.
# Liegt bewusst NICHT unter public/ — was dort liegt, wird beim Bauen
# mit ausgeliefert. Ins Repository geht nur geheim.json.
daten/

# Das Passwort selbst niemals einchecken.
.passwort
